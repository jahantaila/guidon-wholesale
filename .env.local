# ─── Supabase ──────────────────────────────────────────────────────────────────
# Real Supabase project: zhopxqvaienwjexbmmgz
# Project URL and publishable (anon) key are configured below.
#
# !! ACTION REQUIRED: Add the service_role key before the app can use Supabase.
#    Get it from: Supabase Dashboard → Settings → API → service_role (secret key)
#    Also run supabase/schema.sql and supabase/seed.sql in the SQL Editor.
#
# Until SUPABASE_SERVICE_ROLE_KEY is set (not "placeholder"), the app uses
# local JSON files as a fallback so it remains functional.
NEXT_PUBLIC_SUPABASE_URL=https://zhopxqvaienwjexbmmgz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_5XdUpeUbR7HBthIWaOcIsA_7xlmr250
SUPABASE_SERVICE_ROLE_KEY=placeholder

# ─── Admin portal ──────────────────────────────────────────────────────────────
# Password to access the /admin section.
ADMIN_PASSWORD=guidon2026

# ─── Customer portal (demo credentials) ───────────────────────────────────────
# These are stored in data/customers.json for file-based mode.
# When Supabase is configured, create these users via:
#   Supabase Dashboard → Authentication → Users → Add User
#   Email: jahan@derbydigital.us  Password: DerbyDigital123!
